/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var AboutRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return AboutRouter;
});
